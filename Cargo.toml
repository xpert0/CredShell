[package]
name = "kernyx"
version = "0.1.0"
edition = "2021"
author = "xpert0"
description = "Local-first, zero-cloud, manually activated TOTP and passkey authenticator"

[dependencies]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
